{
	"name": "server",
	"type": "module",
	"main": "src/index.ts",
	"scripts": {
		"build": "tsdown",
		"check-types": "tsc -b",
		"compile": "bun build --compile --minify --sourcemap --bytecode ./src/index.ts --outfile server",
		"dev": "bun run --hot src/index.ts",
		"start": "bun run dist/index.js"
	},
	"dependencies": {
		"@elysiajs/cors": "^1.3.3",
		"@sync-station/auth": "workspace:*",
		"@sync-station/db": "workspace:*",
		"@sync-station/env": "workspace:*",
		"better-auth": "catalog:",
		"dotenv": "catalog:",
		"elysia": "^1.3.21",
		"zod": "catalog:"
	},
	"devDependencies": {
		"@sync-station/config": "workspace:*",
		"@types/bun": "catalog:",
		"tsdown": "^0.16.5",
		"typescript": "catalog:"
	}
}
